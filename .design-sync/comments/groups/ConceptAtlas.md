---
category: C · Atlas
---

Redesign C — a camera/map model. A persistent right-edge atlas (a shrunken live map of the page with every comment pinned on it, plus a viewport lens) is the navigator: tap a pin and the page smooth-flies/zooms to that element while its thread docks in clear space beside it — never on top. Traverse by gliding the camera.
