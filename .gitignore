# deps / build
node_modules/
dist/
console/dist/
console/node_modules/
.wrangler/
# 由 `pnpm gen:assets` 生成(static/* + skill.md 内联);cf:dev/cf:deploy 自动跑
src/generated/

# runtime data
data/
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.*
!.env.example
.dev.vars
.devdata

# e2e artifacts
e2e/node_modules/
e2e/test-results/
e2e/playwright-report/
e2e/.playwright/

# editor / os
.idea/
.vscode/
.DS_Store

# local working dirs (design sync / scratch) — not part of the project
.design-sync/
.design-review/
.ds-sync/
brand/
ds-bundle/
ds-bundle-v1/
ds-bundle-comments/

# local-only mise env (dev credentials, never commit)
mise.local.toml
